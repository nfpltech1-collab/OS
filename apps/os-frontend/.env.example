# Target OS backend for Next.js /api rewrites.
# For local development keep this at localhost:3001.
OS_BACKEND_URL=http://localhost:3001

# Optional client-side fallback target used when /api proxy calls fail.
# Usually keep this same as OS_BACKEND_URL.
NEXT_PUBLIC_OS_BACKEND_URL=http://localhost:3001
